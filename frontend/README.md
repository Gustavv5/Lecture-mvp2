How the project is run

1. Create and activate a virtual environment - python m- venv venv
2. Activate it through powershell using - Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\venv\Scripts\activate
3. Install independencies - pip install openai-whisper pytest
4. Run the app - python app.py
6. Run tests - pytest -s
